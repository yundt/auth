<% if logged_in? %>
  logged in

  <%= link_to "log out", session_path, :method => :delete %>
<% else %>
  not logged in

  <%= form_tag session_path, :method => :post do %>
    <%= password_field_tag %>
    <%= submit_tag "log in" %>
  <% end %>
<% end %>
